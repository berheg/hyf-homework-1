// - Write a function, that takes two inputs (you first and last name), and returns the concatenation

function fullName (firstName, lastName) {
    let name = firstName + ' ' + lastName;
    return name;
}

// let name = fullName ('Kseniia', 'Zar');
// console.log(name);

console.log(fullName('Ksu', 'Zar'))